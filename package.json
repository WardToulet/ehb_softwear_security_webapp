{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "lerna run build",
        "start": "cd ./packages/server && yarn start"
    },
    "devDependencies": {
        "lerna": "^2.11.0"
    }
}
