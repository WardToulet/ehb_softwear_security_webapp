{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "lerna run build",
        "start": "cd ./packages/server/dist && node index.js"
    },
    "devDependencies": {
        "lerna": "^2.11.0"
    }
}
